export function Writing() {
  return (
    <section className="space-y-6">
      <h2 className="text-2xl font-semibold">Writing</h2>
      <div className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-muted/10">
        <span className="text-sm italic text-muted">On the way...</span>
      </div>
    </section>
  );
}
